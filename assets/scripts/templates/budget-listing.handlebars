{{#each budgets as |budget|}}

  <!-- {{> partial}}
  Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <ul data-id="{{budget.id}}"><li>Amount: {{budget.Amount}}</li>
  <div>
    {{#if budget.amount}}
      <li>{{budget.amount}}</li>
      <li>{{budget.id}}</li>
      <button data-id="{{budget.id}}remove"type="button"><li>Remove</li></button>
    {{else}}
      No author provided.
    {{/if}}
  </div>
</ul>
{{/each}}
