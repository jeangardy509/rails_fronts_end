{{#each budgets as |budget|}}

  <!-- {{> partial}}
  Don't uncomment yet -->

 <!-- Developers, why are we using data-id and not just id? -->
  <ul data-id="{{book.id}}"><li>Title: {{book.title}}</li>
  <div>
    {{#if budget.id}}
      <li>{{budget.date}}</li>
      <li>{{budget.id}}</li>
      <button data-id="{{book.id}}remove"type="button"><li>Remove</li></button>
    {{else}}
      No author provided.
    {{/if}}
  </div>
</ul>
{{/each}}
